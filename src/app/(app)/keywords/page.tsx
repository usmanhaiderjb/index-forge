import { KeywordsView } from "./keywords-view";

export const metadata = {
  title: "Keyword Research & Corpus Explorer | IndexForge",
  description: "Search 720,000+ indexed keywords across App Store and Google Play with live volume popularity, difficulty scores, and opportunity metrics.",
};

export default function KeywordsPage() {
  return <KeywordsView />;
}
